# 变更记录（CHANGELOG）

> 记录 PulseAI（原 ai-hotmonitor）从 v1 到 v3 的全部功能迭代、架构修改与技术栈演变
> 最后更新: 2026-08-07

---

## 版本时间线

| 版本 | 提交 | 阶段 |
|------|------|------|
| v1 | —（历史版本） | 全量爬取 → 按热度过滤，无关键词驱动 |
| v2 | `b88c4a5` | 关键词驱动的 AI 热点监控（9 源 + 四阶段管线 + 三级分类） |
| v2.1 | `15b0596` | UI 重构（Aceternity 风格开发者美学） |
| v3 | `a3bbc43` `49e0c37` `14e73ba` | 信息源优化 + 无界热度体系 + 管线并发优化 + 内容过滤 + 谣言标记 |
| v3.1 | `bf22ab6` `ed931f3` | 话题排序/筛选增强 + 7 天活跃口径 + 界面视觉升级 |
| v3.2 | `476641a` | 价值信号展示（理由/置信度/互动量/榜单排名/值得关注）+ 一行一话题 UI + 扫描进度 |
| v3.3 | `aac7c2a` | 相关性优化：全渠道查询扩展 + 意图上下文 + 摘要判定 + 测试评估体系 |
| v3.4 | — | 相关性调优（快路径移除）+ 管线资源优化（verifyRetry）+ 官网/公司页过滤治理 + 7 天查重合并 + 2 小时扫描 + 关键词软删除恢复 |

---

## 一、v1 → v2：重构为关键词驱动

### 核心变化

| 维度 | v1 | v2 |
|------|-----|-----|
| **采集策略** | 全量爬取 → 按热度过滤 | **关键词驱动**：爬取 → AI 判定相关性 → 只保留相关 |
| **保留条件** | heatIndex ≥ 50 | AI 确认相关 **且** (热度 ≥ 50 **或** 增速超阈值) |
| **话题分类** | 无分类 | 三级：🚀爆发 / 🔥热点 / 📈潜力 |
| **手动控制** | 仅 API 端点 | 前端「立即检查」按钮（加载态 + 结果反馈） |
| **噪音过滤** | 依赖 AI 验证 | 关键词不相关直接丢弃（在验证之前） |
| **技术栈** | 未记录 | React 18 + Vite + Express 5 + Prisma/SQLite + Socket.io + OpenRouter |

### v2 新增功能
- 9 个数据源（微博/知乎/百度/头条/B站/36氪/GitHub/Twitter/网络搜索）
- OpenRouter AI 管线（聚类 → 验证 → 增速 → 关键词匹配）
- 三级话题分类 + 增速评分（velocity_score）
- Web Push 浏览器通知 + 分级告警
- Agent Skill 端点（供其他 AI 调用）

### v2.1 UI 重构
- Aceternity 风格的开发者美学（卡片/辉光/扫描线）
- 赛博朋克设计令牌（Tailwind v4 @theme）

---

## 二、v2 → v3：信息源优化与无界热度体系

### 2.1 信息源重构（重大）

| 变更 | 原因 |
|------|------|
| ❌ **删除 Twitter** | twitterapi.io 无 API 额度 |
| ❌ **删除 Google News** | news.google.com 国内不可达（100% 超时），Bing News RSS 实测不可行 |
| ✅ **新增 Hacker News** | hn.algolia.com 可访问，英文技术社区一手讨论 |
| ✅ **B站双通道** | 热榜 + 关键词搜索（buvid3 cookie 解决 412 风控） |
| ✅ **搜狗双通道** | 热词榜 + 关键词搜索（验证码自动降级） |
| ✅ **Bing/通用搜索全关键词** | 原 30 分钟只查 1 个关键词（13 个词 6.5 小时才轮一遍）→ **每轮查全部** |
| ⚠️ 微博保持热榜 | 搜索接口被风控（432/403），无法免登录搜索 |

**修复的关键问题**：Twitter 爬虫使用硬编码查询（从不使用用户关键词）+ 错误被吞导致源状态失真。

### 2.2 AI 管线修复（隐藏根因）

| Bug | 影响 | 修复 |
|------|------|------|
| **OpenRouter SDK 调用格式错误** | **AI 语义判定从未生效**（项目最大隐藏 bug）——只有标题字面含关键词的才被保留 | `chat.send({ chatRequest })` Speakeasy 包装格式 |
| DeepSeek 返回 ```json``` 代码围栏 | JSON.parse 静默失败 → 批量判定全 null → 话题永远进不了列表 | 解析前剥离围栏 + 失败打日志 |
| AI 请求无超时 | 管线卡死（CPU 静止、无日志） | 120s 超时保护（Promise.race） |
| 手动触发与定时器并发 | 双管线并发 → P2025 重复删除 | triggerCrawl 共享 running 锁（进行中返回 409） |
| 删除时告警外键冲突 | P2003 | Alert.topicId 级联删除 |
| 逐条 AI 调用 | 管线 500 条需 88 分钟 | 批量判定（12 条/批）+ 批量验证 |

### 2.3 热度体系重构（核心）

**问题**：原 heatIndex（0-100）是源内归一化相对分——量级被抹平（哪吒爆火 3000 万互动与普通话题都是 100 分）、跨源不可比。

**v3 双热度体系**：

| 概念 | 字段 | 范围 | 公式 |
|------|------|------|------|
| 热力值 | heatIndex | 0-100 | 源内归一化（保留，用于阈值判定） |
| **热度值** | heatScore | **0 ~ 数千无界** | 真实源：√(互动加权)÷2；代理源：热力值×15 |

- **24 小时半衰期衰减**（爆款一周后自然回落，不永久霸榜）
- **增速 = (当前热度值 − 初始热度值) ÷ 初始 × 100**（初始 = 首次观测，不受衰减干扰）
- **增速分 = 增速% × √(热度值+1) × 来源系数 ÷ 10**
- 阈值：热点 = 热力值 ≥ 80（原 50，防搜索源排名分泛滥）；爆发 = 增速分 ≥ 100；潜力 = 增速分 ≥ 30
- **级别每轮全量重算**（原只升级不降级 → 热度回落自动降级）

**演进过程**（用户反馈驱动的迭代）：
1. 初版：log 权重 → 小基数话题增速虚高（热度 30 的话题拿到 89 分）
2. 改为 √ 权重 → 高热度加速话题优先（89.5 → 33.4）
3. 代理源从锚点映射改为**线性 ×15**（用户指定：100 热力值 = 1500 热度值）
4. 增速从"相邻轮对比"改为"当前−初始"（用户指定，不依赖观测窗口）

### 2.4 内容过滤（新增）

[content-filter.ts](src/server/crawlers/content-filter.ts) 入库前规则过滤：

| 层 | 规则 |
|----|------|
| 标题黑名单 | 百科/词典/单词/是什么/什么是/什么叫/官网/官方下载/下载(?!量)/指南/教程/保姆级/新闻资讯/娱乐看猫眼… |
| URL 黑名单 | baike.baidu.com / zhihu.com/topic / dramx.com / 163.com/dy/media / imdb.com… |
| 品牌-域名检测 | 标题词 ∩ 域名核心词 + 首页/下载/文档路径（"DeepSeek \| 深度求索"→deepseek.com） |

**防误杀设计**：知乎问答/深度文章、测评/实测、含"下载量"的新闻均保留。

**迭代记录**（用户逐轮反馈补充）：
- 初版 12 条规则（百科/词典/官网/教程）→ 存量清理 94 条
- 补充：竖线品牌页（"DeepSeek | 深度求索"）、App_、域名解析 bug 修复（.com.cn 双后缀）
- 补充：中文视频站门户（豆瓣/爱奇艺/哔哩哔哩）→ 再清 7 条
- 补充：下载/指南/镜像站/官方文档路径 → 再清 27 条
- 补充：什么是/什么叫/门户首页（猫眼/全球半导体观察/快科技标签页）→ 再清 18 条

### 2.5 内容验证精简

| 变更 | 原因 |
|------|------|
| verify 从全量 → **仅对可能入榜话题**（热力≥80 或预估增速≥30） | 省时（用户要求"不属于爆发/热点/潜力的话题不用验证"） |
| 输出从 5 字段 → **3 字段**（classification/confidence/isActionable） | 移除摘要/内容分类，缩短响应 |
| **摘要生成移除** | 用户取消（省时），详情页不再显示 AI 摘要 |
| **isRumor 谣言标记新增** | 入榜话题显示 ⚠️ 疑似谣言 / √ 不是谣言（红色/绿色徽章） |

### 2.6 性能优化（实测）

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| AI 调用粒度 | 逐条（~8s/条） | 批量 12 条/批（~32s/批） |
| 管线并发 | 串行 | **并发 2 路**（墙钟 = max 而非 sum） |
| 管线一轮耗时 | ~22 分钟（曾挂死 1.5h+） | **~5-10 分钟**（DeepSeek 负载波动） |
| 内容过滤 | 无 | 搜索源噪音 ~65% 入库前拦截 |

### 2.7 关键词体系

- `Keyword.searchQueries` 字段（英文搜索词缓存）
- 内置映射表（AI大模型→LLM 等，大小写不敏感）+ LLM 自动生成未命中词并缓存
- **新关键词自动生效**：创建时立即生成 → 下一轮爬取全源覆盖

### 2.8 前端迭代

| 变更 | 说明 |
|------|------|
| 热度值/热力值双展示 | 术语统一（排序选项、详情页 StatPill、峰值热力值） |
| KPI 卡片可点击 | 爆发/热点/潜力 → 跳转 `/topics?tier=xx` 自动筛选 |
| 话题浏览默认按热度值排序 | + URL 参数同步（?tier=） |
| 热度趋势图 | heatIndex → **heatScore**（TopicHistory 加字段） |
| #关键词徽章 | 列表 + 详情显示关联关键词 |
| ⚠️ 谣言标记 | isRumor 红色/绿色徽章 |
| 移除 AI 摘要/分类徽章 | 配合 verify 精简 |

---

## 三、v3 → v3.1：话题浏览效率与界面体验升级

### 3.1 排序增强

| 排序 | 字段 | 说明 |
|------|------|------|
| **综合推荐（默认）** | `recommendScore` | 级别权重 + 增速 + 热度 + 新鲜度，正在爆发/上升的排前面 |
| 热度值 | `heatScore` | 保留 |
| 增速 | `velocityScore` | 保留 |
| 热力值 | `heatIndex` | 保留 |
| 连续上榜 | `mentionCount` | 原名"提及"，含义不变（同一来源连续多轮采集到） |
| 最新发布 | `publishedAt` | 按话题原始发布时间（B站/36氪/HN 可采集） |
| 最新发现 | `lastSeenAt` | 按最近一次被抓取时间 |

### 3.2 筛选增强

- 发现时间范围：最近 1 小时 / 6 小时 / 24 小时 / 7 天 / 全部（按首次发现 `firstSeenAt`）
- 关键词多选：**每新增一个关键词自动出现对应筛选选项**，带话题计数
- 信息来源多选：8 个数据源，带话题计数
- 级别筛选保留；移除与实际数据不符的"分类"下拉
- 全部筛选条件同步到 URL，刷新/分享不丢状态

### 3.3 7 天活跃窗口（统一口径）

- KPI 卡片、实时话题网格、增速榜统一为"近 7 天仍在采集到"的话题
- 话题超过 7 天未再被抓到 → 自动降级为无级别（修复旧级别永不过期的"僵尸级别"问题）
- 存量数据已按 7 天规则重新定级

### 3.4 发布时间与发现时间

- `Topic.publishedAt` 字段：原始发布时间（可空，B站/36氪/Hacker News 提供）
- 话题列表卡片标注 `发布 xx-xx xx:xx · 发现 xx-xx xx:xx`
- "最新发布"排序时未知发布时间的话题自动排最后

### 3.5 数据库变更

| 迁移 | 内容 |
|------|------|
| `add_published_at_and_recommend_score` | 新增 `publishedAt` / `recommendScore` 字段及索引 |
| `backfill_recommend_score` | 存量话题回填综合推荐分（首版对毫秒时间戳处理有误） |
| `fix_backfill_recommend_score` | 修正回填 SQL，按 epoch 毫秒正确计算新鲜度 |

### 3.6 修复

- 话题排序字段增加白名单校验（非法字段不再 500）
- 36氪 API 被拦截/解析失败时自动降级 RSS（原 JSON 解析失败不会触发兜底）
- Express 5 `req.params` 类型错误（`string | string[]`）
- Agent 接口重复关键词时报错引用未定义变量

### 3.7 UI 视觉升级（UI UX Pro Max）

- 深色金融仪表盘风格：Space Grotesk 标题字体 + 绿→青渐变品牌色 + 玻璃拟态卡片 + 环境光晕动效
- 统一 Lucide 风格 SVG 图标，替换 KPI/徽章/时间标注中的 emoji
- KPI 卡片：级别渐变顶条 + 数字滚动动画；话题卡片：级别色条、渐变热度条、增速正负着色
- 热度趋势图从折线图升级为渐变面积图；空状态/按钮/输入框统一打磨
- 设计系统文档 `design-system/pulseai/MASTER.md` 同步更新

---

## 四、v3.1 → v3.2：价值信号展示与扫描进度

### 4.1 核心变化

| 维度 | v3.1 | v3.2 |
|------|------|------|
| **相关性展示** | 仅 #关键词 徽章 | AI 匹配理由（15-25 字）+ 置信度；列表默认收起理由、点击展开，置信度常驻 |
| **内容验证** | classification + confidence + isActionable（confidence/isActionable 未使用） | **精简为 isVerified / isRumor / isActionable 三个布尔**，省 token、响应更快、语义等价 |
| **值得关注** | 无 | isActionable 展示为「值得关注」徽标 |
| **互动数据** | rawHeat 加权汇总（未展示） | **engagement JSON 明细**：微博热度/标签、百度搜索指数、B站播放/弹幕/评论/收藏/点赞、HN 分数/评论；列表主指标 + 详情分项 |
| **榜单排名** | 仅详情页来源排名 | 微博/百度/B站热榜前 10 显示 `#N 来源`，其他来源不显示序号 |
| **话题行 UI** | 首页多列网格 + 列表卡片 | **一行一话题 TopicRow 全宽行**（Dashboard 与 TopicsPage 共用），热度值/增速同字号大字 |
| **热力值** | 话题行展示 0-100 | 移出行内展示（保留分级阈值与详情页指标） |
| **扫描可见性** | 无全局状态 | **全局扫描进度条**：百分比 + 阶段 + 当前来源 + 已发现条数；完成短暂提示 |
| **废弃字段** | aiSummary/aiCategory/rawHeat 持续写入 | **停止写入并移除 API 展示**（数据库列保留，待后续 migration 删除） |

### 4.2 数据与接口

- 迁移 `add_topic_engagement_signals`：Topic 新增 `matchReason` / `matchConfidence` / `engagement` / `isActionable`
- 新增 `GET /api/v1/crawl/status` 与 Socket `crawl_status` 事件
- 爬虫统一输出 `CrawlerItem.engagement`；调度器维护 `CrawlStatus`（爬取 0-80%、AI 82-98%、完成 100%）
- topics/agent 路由移除 aiSummary/aiCategory/rawHeat 响应字段，category 过滤改用 matchedKeyword
- 项目名由 ai-hotmonitor 更名为 PulseAI（物理目录名保留 ai-hotmonitor，避免影响工作区与远端配置）

### 4.3 前端

- 新增 `TopicRow`（Dashboard 与 TopicsPage 共用）、`ScanStatusBar`（全局进度条）、`utils/format.ts`（热度/互动量格式化）
- 详情页新增「AI 相关性分析」（理由 + 置信度）与「互动数据」分项
- 修复 Socket 监听清理返回类型（useSocket）

---

## 五、v3.2 → v3.3：相关性优化与测试评估体系

### 5.1 核心变化

| 维度 | v3.2 | v3.3 |
|------|------|------|
| **查询扩展** | 仅 HN 使用英文搜索词 | **全渠道启用**：国内渠道中文扩展词（原词 + 变体跨轮轮换），HN 英文翻译词轮换；记录命中词 `searchQuery` |
| **关键词意图** | 无 | **intentContext**：建词/改名时 AI 生成投资语境说明并缓存，调度器启动懒回填；用于扩展词生成与相关性判定 |
| **判定输入** | 仅标题 + 关键词列表 | **标题 + 摘要 + 来源 + 意图上下文**，结构化多行 prompt；prompt 构建抽为纯函数 |
| **快路径** | 标题含关键词直接放行 | **移除**，全部经 AI 复核；AI 失败留待下轮（不误删） |
| **测试评估** | 无 | 单元/集成测试 + 68 条黄金用例 + `npm run eval:relevance`（P/R/F1/混淆矩阵/错误样例）+ AI 生成候选用例 |
| **内容过滤** | 教程/保姆级等 | 补充“技巧”等教程类噪音拦截 |

### 5.2 数据与接口

- 迁移 `add_search_context_and_snippet`：`Keyword.intentContext / zhExpansionQueries`，`Topic.snippet / searchQuery`
- 爬虫：B站/搜狗/Bing/通用搜索改用 `selectQueriesForChannel('zh')`（原词 + 1 个变体轮换），HN 用 `'hn'`（英文词轮换）；各来源摘要入库
- 关键词创建/改名时预生成意图、中文扩展词与英文搜索词；调度器启动懒回填
- 新增 npm scripts：`test` / `eval:relevance` / `eval:golden:generate`

### 5.3 相关性评估历程（2026-08-07）

| 步骤 | 改动 | 测试集 | 精确率 | 召回率 | F1 | 说明 |
|------|------|--------|--------|--------|-----|------|
| 基线 | 扩展查询/意图/摘要上线后 | 16 | 72.7% | 100% | 84.2% | 3 个 FP 全部来自“标题含词即放行”快路径 |
| 1 | 移除快路径，全部 AI 复核 | 16 | 88.9% | 100% | 94.1% | 剩 1 FP：AI绘画比赛（意图不明确） |
| 2 | AI大模型意图排除泛 AI 创作 | 16 | 100% | 87.5% | 93.3% | 新 FN：AI 忽略摘要（标题“AI新品”+摘要描述模型能力） |
| 3 | 摘要改为结构化多行展示 | 16 | 88.9% | 100% | 94.1% | 新 FP：新能源车展（营销内容） |
| 4 | 新能源意图排除车展 + 通用营销规则 | 16/68 | 89.5% | 91.9% | 90.7% | 扩充至 68 条；7 错误（含 1 条编码损坏） |
| 5 | 修复 gen-026 编码损坏 | 68 | 88.9% | 100% | 94.1% | 华为 6 条 retried，实际评估 62 条；剩 4 个边界 FP |
| 6 | 半导体/DeepSeek/特斯拉意图排除 + 通用规则 + 过滤“技巧” + 修正 gen-047 用例 | 68 | 待评估 | — | — | 针对回收/教程/改装/多关键词误标 |

### 5.4 修复与注意

- 修复测试集合并脚本中文编码损坏（gen-026 标题/摘要变 `????`）
- 修正 gen-047 用例：去掉“华为/小米”等被监控品牌，避免“命中其它关键词”导致的评估误标
- 华为一轮 6 条 retried（AI 调用整批失败），重跑后恢复——评估中的拒答率需长期关注

---

## 六、v3.3 → v3.4：相关性调优、资源优化与数据治理

### 6.1 相关性判定调优

- **移除快路径**：标题包含关键词不再直接放行，全部经 AI 复核（评估实测精确率 72.7% → 88.9%）
- **结构化摘要 prompt**：标题/摘要/来源/意图分开展示，摘要作为标题不足时的主要依据
- **意图上下文显式排除**：AI大模型排除泛 AI 创作；半导体排除二手回收/维修；DeepSeek 排除教程技巧；特斯拉排除第三方改装；新能源排除车展营销
- **相关性批次 maxTokens 2048 → 4096**，降低 JSON 截断导致的整批 retried
- 评估历程补充见 5.3：步骤 6（意图排除 + 过滤“技巧” + gen-047 修正）已上线，最终指标待重跑回填

### 6.2 管线资源优化（AI 调用量）

- 新增 `Topic.verifyRetry`：verify 失败的话题标记待重试
- **管线只处理两类话题**：`matchedKeyword IS NULL`（未判定/相关性重试）或 `verifyRetry = true`（只补 verify）
- 已判定话题不再重复送 AI，只由爬虫连续上榜 +1、`classifyTiers` 本地重算级别，显著降低调用量与 429 概率

### 6.3 内容过滤与数据治理

- **品牌域名强规则**：已知品牌域名（华为/小米/苹果/DeepSeek/Claude 等，含镜像域名）除 news/blog/press 外一律视为官网低价值页
- **中文品牌别名映射**：标题含中文品牌名即可命中（解决“华为-构建万物互联的世界”漏网）
- **公司注册/工商页规则**：`XX有限公司/有限责任公司/股份有限公司` 结尾拦截；`公司名+商城/官网/首页` 组合拦截
- 存量清理：两批共删除 21 条官网/公司页/镜像噪音（华为、小米商城、DeepSeek 山寨站、Claude 中文镜像等）
- **话题查重窗口 2 小时 → 7 天**：7 天内重新出现合并更新，超过 7 天才视为新话题
- 存量重复合并：80 组同源同标题重复记录合并（历史/告警迁移、mentionCount 累加），前端按标题去重兜底

### 6.4 调度与关键词管理

- 自动扫描间隔 30 分钟 → **2 小时**（7200000ms），扩展词轮换周期同步
- **关键词软删除**：删除 = 停用 + `deletedAt` 标记 + 隐藏相关话题（`Topic.isHidden`）；同名重新添加自动恢复关键词并解除话题隐藏
- 所有话题查询限定“仍存在的关键词”，并回填隐藏 27 条历史孤儿标签话题（旧物理删除遗留）

### 6.5 数据与迁移

- `add_verify_retry`：Topic.verifyRetry
- `keyword_soft_delete_and_hide_topics`：Keyword.deletedAt / Topic.isHidden
- 新增维护脚本：`scripts/cleanup-official-pages.ts`、`scripts/merge-duplicate-topics.ts`

---

## 七、技术栈演变

| 层 | v2 | v3 | 说明 |
|----|-----|-----|------|
| 爬虫 HTTP | got 15 | got 15（不变） | 保持（非 axios） |
| AI 模型 | gemini-2.5-flash 分级 | **deepseek-v4-flash 统一** | 全管线单模型 |
| AI 调用方式 | `chat.send(request)`（错误） | `chat.send({ chatRequest })` + 120s 超时 + 围栏剥离 | SDK v1.2.x Speakeasy 格式 |
| 开发模式 | `tsx watch`（concurrently 下挂起） | **`tsx watch < NUL`** | stdin 空设备修复 Windows 无 TTY 挂起 |
| 数据源 | 9 个 | **8 个** | 删 twitter/google，加 HN |
| 数据库字段 | 基础 | +searchQueries/heatScore/prevHeatScore/history.heatScore/isRumor | 6 个 migration |
| 新增模块 | — | content-filter / keyword-queries / hacker-news | 3 个新文件 |
| 前端风格 | v2 基础 | Aceternity 风格 + 双热度展示 + 谣言/关键词徽章 | — |

---

## 八、关键修复清单（按严重性）

1. **OpenRouter SDK 调用格式错误** → AI 语义判定从未生效（全项目影响最大的 bug）
2. **DeepSeek JSON 代码围栏** → 批量判定静默失败、管线"看似运行实则不动"
3. **AI 请求无超时** → 管线无限挂死（CPU 静止、无错误日志）
4. **手动触发与定时器并发** → 双管线 P2025 重复删除
5. **Alert 外键无级联** → 删除话题 P2003
6. **twitter 硬编码查询 + 吞错** → Twitter 源从未抓取用户关键词相关内容
7. **Google 假死源** → 48s 超时返回 0 条但状态显示 ok
8. **bing 只查第一个关键词** → 其余 12 个关键词从不查询
9. **热力值阈值 50 过低** → 搜索源排名分泛滥（103 个"热点"）
10. **tier 只升不降** → 增速回落后仍标记潜力
11. **log 权重增速** → 小基数话题增速虚高（89 分）

---

## 九、遗留与后续建议

- 增速分阈值（30/100）为常量，可基于真实数据分布校准
- 跨源话题聚类未实现（同一话题多源为独立记录）
- 微博搜索通道被风控（如需可接入登录态 cookie）
- 搜狗搜索通道受验证码频率限制（自动降级）
- 谣言判定 prompt 可调保守度（当前对强断言标题偏敏感）
- 废弃列 aiSummary/aiCategory/rawHeat 已停写，待后续 migration 删除
- 热力值已移出行内展示，但仍作为分级阈值核心，若移除需重构定级标准
- 黄金用例中 AI大模型/芯片/新能源 数量偏少（各 2-3 条），可用 `npm run eval:golden:generate AI大模型 芯片 新能源` 补充
- 单次评估存在模型波动，建议同一版本跑 2-3 次取均值
- 相关性评估“retried 率”需长期监控（整批失败会导致指标失真）
- 相关性评估步骤 6 的最终指标待重跑回填
- 36氪被火山引擎 WAF 拦截（浏览器可达、脚本客户端被拦），修复方案已出但未实施
- OpenRouter 429：已通过减少重复调用缓解，429 退避重试尚未实施
- 跨源话题聚类仍未实现，前端按标题去重为临时兜底
