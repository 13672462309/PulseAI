# 热点监控工具 — 需求文档

> 版本: v2.0 | 日期: 2026-08-02 | 状态: 重构中

---

## 1. 项目背景

为投资者打造 **关键词驱动** 的 AI 热点监控工具，围绕用户设定的关键词，从多个平台自动采集信息，由 AI 判定相关性并评估热度，第一时间发现真正的热点和即将爆发的潜力话题。

---

## 2. 核心工作流（v2 重构）

```
用户设定关键词
      │
      ▼
┌─────────────────────────────────────────────┐
│ 爬虫采集（9 个数据源）                         │
│ 每 30 分钟自动执行，或用户点击「立即检查」手动触发  │
└──────────────────┬──────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│ AI 关键词相关性判定（OpenRouter）               │
│ 对每条爬取内容，判断是否与数据库中任一激活关键词     │
│ 语义相关。不相关的 → 丢弃                       │
└──────────────────┬──────────────────────────┘
                   ▼
          ┌───────┴───────┐
          ▼               ▼
   ┌──────────┐    ┌──────────────┐
   │ 热度 ≥ 50 │    │ 热度 < 50     │
   │          │    │ 但增速超阈值   │
   │ → 保留   │    │ → 保留+潜力标记 │
   │ 标记:🔥  │    │ 标记:📈       │
   └──────────┘    └──────────────┘
          │               │
          └───────┬───────┘
                  ▼
         ┌──────────────┐
         │ 热度 ≥ 50    │
         │ 且增速超阈值  │
         │ → 额外标记:🚀 │
         └──────────────┘
                  │
                  ▼
            存入数据库 → 前端展示 + 通知推送
```

### 话题三级分类

| 级别 | 条件 | 标记 | 含义 |
|------|------|------|------|
| **爆发** 🚀 | AI 确认关键词相关 + heatIndex ≥ 50 + velocity_score 超阈值 | 红色高亮 | 已是热点且仍在加速——最值得关注 |
| **热点** 🔥 | AI 确认关键词相关 + heatIndex ≥ 50 | 正常展示 | 已被验证的当前热点 |
| **潜力** 📈 | AI 确认关键词相关 + heatIndex < 50 但 velocity_score 超阈值 | 特殊标记+琥珀色 | 尚未广泛传播但增速极快——抢先布局 |

不满足以上任一条件的 → **丢弃**（不存入数据库）

---

## 3. 用户故事

### US-1: 关键词驱动的热点发现

> 我设定关键词后，系统只关注和我的关键词真正相关的内容，噪音内容（不相关热搜/娱乐八卦）直接过滤。

**验收标准：**
- 爬虫采集的每条内容都必须经过 AI 关键词相关性判定
- 与所有激活关键词均不相关的内容直接丢弃
- 前端展示的话题 100% 都与用户关键词相关
- 关键词匹配采用字符串包含 + AI 语义匹配双通道

### US-2: 三级话题分类

> 我需要看到三种话题：正在爆发的最热话题、已经热起来的话题、以及正在快速上升可能爆发的话题。

**验收标准：**
- 热度 ≥ 50 → "热点"标记
- 热度 < 50 但增速超阈值 → "潜力"标记（琥珀色区分）
- 两者同时满足 → "爆发"标记（红色高亮，最高优先级告警）

### US-3: 手动立即检查

> 我不想等 30 分钟，当我看到新闻或感觉有大事发生时，可以点一个按钮立即检查。

**验收标准：**
- 仪表盘首页醒目位置有「立即检查」按钮
- 点击后触发全量爬虫 + AI 管线
- 执行过程中按钮显示加载状态
- 完成后自动刷新数据
- 30 分钟自动调度不受影响

### US-4: 增速阈值可配置

> 潜力话题的判断标准（增速阈值）应该可以调整。

**验收标准：**
- 设置页面可配置增速阈值
- 默认值：velocity_score ≥ 15

---

## 4. 功能需求

### 4.1 数据采集

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|--------|
| F-01 | 多源爬虫 | 微博、知乎、百度、头条、B站、36氪、GitHub Trending | P0 |
| F-02 | Twitter (X) API | 通过 twitterapi.io 获取国际热点 | P0 |
| F-03 | 通用网页搜索 | 以关键词为搜索词爬取搜索引擎结果 | P0 |
| F-04 | 限速保护 | 指数退避、UA轮换 | P1 |
| F-05 | 源健康监控 | 数据源状态追踪 | P2 |

### 4.2 AI 智能分析（核心）

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|--------|
| F-06 | **关键词相关性判定** | 每条内容判断是否与任一激活关键词语义相关。不相关→丢弃 | **P0** |
| F-07 | 内容真实性验证 | 分类：真实/营销垃圾/谣言/低价值 | P0 |
| F-08 | 摘要生成 | AI 生成一句话中文摘要 | P1 |
| F-09 | 热度评分 | 归一化各平台热度到 0-100 统一尺度 | P0 |
| F-10 | 增速计算 | velocity_score = growth_rate × log(heat+1) × source_diversity | P0 |
| F-11 | 三级分类 | 爆发/热点/潜力 自动标记 | P0 |

### 4.3 通知推送

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|--------|
| F-12 | 浏览器通知 | Web Push API + Service Worker | P0 |
| F-13 | 分级告警 | 🚀critical / 🔥warning / 📈info | P0 |
| F-14 | 告警去重 | 同话题30分钟内不重复通知 | P1 |

### 4.4 前端界面

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|--------|
| F-15 | **立即检查按钮** | 仪表盘首页醒目按钮，点击手动触发全流程 | **P0** |
| F-16 | 指挥中心首页 | KPI行 + 三级话题网格 + 告警流 | P0 |
| F-17 | 话题浏览器 | 按分类（爆发/热点/潜力）筛选 | P0 |
| F-18 | 话题详情 | 趋势图 + AI分析 + 关键词关联 | P1 |
| F-19 | 关键词管理 | CRUD + 暂停/激活 | P0 |
| F-20 | 设置页面 | 增速阈值、爬取间隔等配置 | P1 |

---

## 5. 非功能需求

| 编号 | 类别 | 要求 |
|------|------|------|
| NF-01 | 性能 | 立即检查 2 分钟内完成全流程 |
| NF-02 | 可用性 | 单源故障不影响其他源采集 |
| NF-03 | 数据保留 | topic_history 保留 30 天 |
| NF-04 | 安全 | API Key 通过 .env 配置 |
| NF-05 | 成本 | AI 日均消耗 < $0.50 |

---

## 6. 与 v1 的关键差异

| 维度 | v1 | v2 |
|------|-----|-----|
| **采集策略** | 全量爬取 → 按热度过滤 | **关键词驱动**：爬取 → AI判定相关性 → 保留相关 |
| **保留条件** | heatIndex ≥ 50 | AI 确认相关 **且** (heatIndex ≥ 50 **或** 增速超阈值) |
| **话题分类** | 无分类 | 三级：🚀爆发 🔥热点 📈潜力 |
| **手动控制** | POST /api/v1/crawl/trigger | **前端「立即检查」按钮**，加载态 + 结果反馈 |
| **噪音过滤** | 依赖 AI 内容验证 | **关键词不相关直接丢弃**（在验证之前） |
